export const BASE_URL =  ''; // 'Proxy in package.json is already set to http://localhost:5000';
export const API_URL = `${BASE_URL}/api`;

export const PRODUCTS_URL = `${API_URL}/products`;
export const USERS_URL = `${API_URL}/users`;
export const ORDERS_URL = `${API_URL}/orders`;
export const PAYPAL_URL = `${API_URL}/config/paypal`;
export const UPLOAD_URL = `${API_URL}/upload`;